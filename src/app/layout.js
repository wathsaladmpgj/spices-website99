import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Premium Spices | Authentic Flavors Worldwide",
  description: "Discover our premium collection of authentic spices sourced from around the world. Quality guaranteed, fresh flavors delivered.",
  keywords: "spices, premium spices, authentic spices, cooking ingredients, organic spices",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
